# 女媧工坊 環境變數範例
# 這些是「shell 環境變數」,不會被自動載入;請於啟動時設定,例如:
#   NUWA_DATA_DIR=~/nuwa-data npm start
# 或先 export 再 npm start。

# Anthropic API 金鑰(也可在介面「設定」填入)
# ANTHROPIC_API_KEY=sk-ant-...

# 資料存放位置。建議指到 repo 外,避免私人資料混進版控。
# NUWA_DATA_DIR=~/nuwa-data

# 伺服器埠(預設 5723)
# PORT=5723

# 登入密碼(部署到公開網路時務必設定!)。
# 設了這個,任何人都得先輸入此密碼才能進入;優先於介面裡的「登入保護」設定,
# 且不落地在資料夾,最適合雲端 / 容器部署。留空則維持無密碼(僅適合本機使用)。
# NUWA_PASSWORD=改成你自己的長密碼

# 對外服務時綁 0.0.0.0(容器已預設);本機直跑預設綁 127.0.0.1 較安全。
# HOST=0.0.0.0

# ── 用 docker-compose 部署到 VPS 時(自動 HTTPS)才需要 ──
# 你的網域(A 記錄要先指向這台 VPS 的 IP)。Caddy 會自動為它申請憑證。
# DOMAIN=nuwa.example.com
# 部署時 NUWA_PASSWORD 為必填;ANTHROPIC_API_KEY 建議一併填(也可日後在介面「設定」填)。
